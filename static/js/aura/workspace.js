const form=document.querySelector('#aura-form');const thread=document.querySelector('#aura-thread');if(form&&thread){form.addEventListener('submit',async(event)=>{event.preventDefault();const input=form.querySelector('textarea');const value=input.value.trim();if(!value)return;const user=document.createElement('div');user.className='aura-message user';user.textContent=value;thread.append(user);input.value='';const response=await fetch(form.action,{method:'POST',headers:{'X-CSRFToken':form.querySelector('[name=csrfmiddlewaretoken]').value,'X-Requested-With':'XMLHttpRequest'},body:new URLSearchParams(new FormData(form))});const data=await response.json();const assistant=document.createElement('div');assistant.className='aura-message assistant';assistant.textContent=data.reply||data.error||'AURA could not respond.';thread.append(assistant);thread.scrollTop=thread.scrollHeight;});}
